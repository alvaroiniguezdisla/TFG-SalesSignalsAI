-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.app_config (
  id integer NOT NULL CHECK (id = 1),
  umbral_similitud numeric DEFAULT 0.82,
  max_emails_ejecucion integer DEFAULT 50,
  delay_entre_emails integer DEFAULT 1,
  ollama_model text DEFAULT 'llama3.1'::text,
  rss_sources jsonb NOT NULL DEFAULT '[]'::jsonb,
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  ai_prompt text DEFAULT ''::text,
  CONSTRAINT app_config_pkey PRIMARY KEY (id)
);
CREATE TABLE public.news_feedback (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  noticia_url_hash text NOT NULL,
  feedback text NOT NULL CHECK (feedback = ANY (ARRAY['like'::text, 'dislike'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT news_feedback_pkey PRIMARY KEY (id),
  CONSTRAINT news_feedback_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.noticias (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  url_hash text NOT NULL UNIQUE,
  url text NOT NULL,
  titulo text NOT NULL,
  resumen text,
  fuente text NOT NULL,
  published_at timestamp with time zone DEFAULT now(),
  scraped_at timestamp with time zone DEFAULT now(),
  categoria_ia text,
  relevancia_ia integer DEFAULT 0,
  resumen_comercial_ia text,
  empresas_clave_ia ARRAY,
  urls_extra jsonb DEFAULT '[]'::jsonb,
  categoria_producto_ia text DEFAULT 'Otros / No Aplica'::text,
  talk_track_ia text DEFAULT ''::text,
  email_draft_ia text DEFAULT ''::text,
  empresas_detalle_ia jsonb DEFAULT '[]'::jsonb,
  CONSTRAINT noticias_pkey PRIMARY KEY (id)
);
CREATE TABLE public.profiles (
  id uuid NOT NULL,
  first_name text,
  last_name text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  email text,
  favorite_companies ARRAY DEFAULT '{}'::text[],
  favorite_categories ARRAY DEFAULT '{}'::text[],
  role text DEFAULT 'user'::text,
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);